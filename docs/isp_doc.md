在isp流程中，有些isp算法只能支持unit8字符格式，因此如果使用unit8或者unit16的格式进入isp的管道，那么isp算法处理中将频繁进行字符位深转换，频繁的字符格式转换将导致最后的结果有数据有比较大的偏差。因此使用float32格式的数据进入isp管道，大部分isp算法能够兼容float32格式。最大程度上减小了数据类型的转换。并且float32的精度完全足够表示16bit的图像数据。
isp管道的输出也为float32，紧接着根据情况可选择转换回unit8或者unit16。